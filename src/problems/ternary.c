/*
 * You can create a oneline if condtion using ternaries. The syntax for ternary
 * is: 'condtion ? expression if true : expression if false'
 *
 * This codes checks if a is one and prints True.
 */
#include <stdio.h>

int main() {
  int a = 1;

  printf("False\n") ? printf("True\n") : a;
}
