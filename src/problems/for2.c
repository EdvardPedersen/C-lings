#include <stdio.h>

int main() {
  int start = 0;
  int end = 10;
  // Printing every number between start and end, including the end points
  for (int i = start; i < end; i++) {
    printf("i = %d\n", i);
  }
}
